# Commit: Dashboard Overhaul
git add frontend/src/pages/Dashboard.tsx frontend/src/pages/Dashboard.css
git commit -m "feat(web): overhaul dashboard into a high-density enterprise command center with dual-axis ComposedCharts and live event streams"

# Push the changes
git push
